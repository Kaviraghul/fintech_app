import 'package:flutter/material.dart';

class UserGoalsSection extends StatelessWidget {
  const UserGoalsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      color: Colors.yellow,
      child: Center(
        child: Text("user goals section"),
      ),
    );
  }
}
